server:
  port: 30402

# spring 配置
spring:
  application:
    name: alinesno-infra-ops-telemetry-boot
  datasource:
    driver-class-name: ru.yandex.clickhouse.ClickHouseDriver
    url: jdbc:clickhouse://127.0.0.1/default
    username: default
    password:
  data:
    redis:
     host: localhost
     port: 6379
     timeout: 10s
  kafka:
    bootstrap-servers:
      - localhost:9092
  devtools:
    restart:
      enabled: true